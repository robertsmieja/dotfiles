#! /usr/bin/env sh

{{ if eq .chezmoi.os "linux" -}}
  {{- if eq .chezmoi.osRelease.idLike "arch" -}}
pacman -Q ansible || sudo pacman -S ansible
  {{- else if eq .chezmoi.osRelease.idLike "debian" -}}
sudo apt update && sudo apt install ansible -y
  {{- end -}}
{{ end }}
